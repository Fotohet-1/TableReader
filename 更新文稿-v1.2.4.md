# TableReader 更新说明 · v1.2.4

> 对比基线：上一版正式外发包 `TableReader-正式版-v1.2.3.zip`。本次为 patch 升级（围读播放体验优化，存档结构与数据不变）。

## 优化

### 围读播放器
- 时码改为逐字符固定宽度，播放速度较快、数字连续变化时不再产生宽度抖动。
- 进度填充与圆球共用同一条轨道，播放、拖动和快速跳转时保持精确对齐。
- 进度改为跟随真实播放时间逐帧推进，圆球移动更加平滑。
- 进度圆球增加轻微蓝色光晕，悬停和拖动时略微增强。

### 旧存档
- 继续围读旧存档时，自动识别并补全音频缺失或为 `0` 的时长，减少载入后播放位置异常。

## 说明

- 版本号：`v1.2.3 → v1.2.4`。
- 应用功能、存档目录与数据结构未变，旧存档可直接继续使用。
- 已生成的 `TableReader.app` 与 `dist/` 已按 v1.2.4 重建。
