# TableReader 更新说明 · v1.2.0

> 对比基线：上一版正式外发包 `TableReader-正式版-v1.1.0.zip`。本次为 minor 升级（新增功能、向后兼容），存档结构与数据不变。

## 一、版本对比

| | v1.1.0 | v1.2.0 |
|---|---|---|
| 顶栏控件 | 深浅开关 + 文本按钮混合 | 统一 32px 圆角矩形，Apple 手册配色 |
| 围读字号 | 固定 | 13–25px 可调，记忆保存 |
| 存档页刷新/状态点 | 蓝色刷新按钮 + 绿点 | 移除，路径框点击打开访达 |
| 项目隐藏 | 无 | 按目录隐藏项目 + 隐藏提示动画 |
| 合成等待横幅 | 单一配色、出现会横移 | 深浅两套配色，居中稳定，渐现渐隐 |
| PWA 顶栏 | 页面同款 `#fcfcfc` | 中性灰 `#7a7a7a` |

## 二、新增

### 1. 围读页字号调节
- 顶栏新增 A/A 字号按钮，13–25px 步进 2px，变更即时生效并写入本地，下次进入自动恢复。

### 2. 存档页隐藏项目
- 每个剧集项目框内新增隐藏按钮，鼠标靠近时才浮现；支持按目录隐藏、查看、解除，隐藏状态写入本地。
- 文件夹路径框改为只读，点击直接在访达中打开。
- 移除原蓝色“刷新”按钮和目录状态点。

### 3. 合成等待横幅
- 浅色、深色模式各一套易读配色；修复出现时的横向偏移；出现与消失改为渐现渐隐。

### 4. 继续围读自动补齐
- 打开旧集时只合成缺失对白，不整集重跑。

### 5. 设置弹窗组件化
- 设置弹窗抽成共享组件 `SettingsModal`，模型、Key、目录、外观等配置统一入口。

### 6. DeepSeek AI 解析剧本提醒
- 配置 DeepSeek Key 后，“解析剧本”按钮显示为“AI 解析剧本”。

## 三、调整

- 顶栏控件统一为 32px 圆角矩形，返回、模型、设置按钮统一；模型状态点并入按钮内。
- 深浅色模式按钮颜色统一。
- 存档页项目框与路径框等宽右对齐，项目内元素右对齐。
- PWA 顶栏主题色由页面同款灰白改为中性灰。

## 四、稳定性与竞态

- Qwen 模型生成迁移到独立子进程 `server_qwen_worker.py`，超时强杀并重建；新增 `/status`，默认超时改为 90s。
- 重生成竞态加固：共享音色、认领、进行中引用；重生成期间后台合成跳过已认领台词，整集拼接等待重生成完成后再执行。
- 整集音频判定收紧，中间态不会误报“生成失败”。

## 五、说明

- 版本号：`v1.1.0 → v1.2.0`（新增功能、向后兼容）。
- 存档目录与数据结构未变，旧存档可直接继续使用。
- 已安装的 PWA 或已拖入 Dock 的 `.app` 可能缓存旧顶栏配色，需重新安装或刷新。

## 六、内部

- Qwen TTS 拆分 worker 子进程，stdout 仅输出 JSON 协议，父进程负责超时与重建。
- 新增设置持久化与 `revealDir` 的单元测试覆盖。
