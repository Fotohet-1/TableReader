# TableReader 更新说明 · v1.2.2

> 对比基线：上一版正式外发包 `TableReader-正式版-v1.2.1.zip`。本次为 minor 升级（新增功能、修复问题，存档结构与数据不变）。

## 一、新增

### 1. 拆行场标识别
- 支持"可选编号 + 地点 + 时间 + 内外"分成多行的场标写法，例如：
  ```
  3-1
  清风山-密室
  日
  内
  ```
- 同时支持 `夜/日`、`日夜`、`内/外`、`内外` 等时间/内外组合写法。
- 火旺剧本 23 集全量验证：607 场全部识别，`日/内` 不再残成旁白。

### 2. 多文件上传逐集独立存档
- 一次上传多集剧本时，每个文件生成独立的存档条目、各自的音频目录与整条音频，不再把多集并成一个文档。
- 修复 `parseScript` 忽略 `idStart` 导致的跨集单元 id 重复问题。

### 3. Qwen 种子文本自动选句
- 新角色的种子文本不再默认使用"第一句台词"，改为自动挑选约 8-10 秒（25-40 字，优先接近 33 字）的稳定台词，降低音色生成不稳定的概率。
- 声音设计页仍可手动修改演示文本并重新生成。

## 二、说明

- 版本号：`v1.2.1 → v1.2.2`。
- 应用功能、存档目录与数据结构未变，旧存档可直接继续使用。
- 已生成的 `TableReader.app` 与 `dist/` 已按 v1.2.2 重建。
