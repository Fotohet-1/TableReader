# 剧本围读助手 · 搭建复盘（part2）

> 承接 `复盘总结-part1.md`，覆盖后一阶段（跨集音色复用、两级存档、深色模式、外发准备）踩过的坑。按“难点 → 根因 → 怎么避免”整理，最后给可套用的原则。配合 `HANDOFF.md`、`README.md`、`外发说明.md` 一起看。

## 服务运行环境

**1. Qwen 用错了 Python，服务“活着”却不能用**

重启 9883 时用了框架 Python（`/opt/homebrew/Cellar/python@3.11/.../Python`），`/tts` 直接 `ModuleNotFoundError: mlx_audio`，前端“生成音色”一按就 500。我一度以为前端没反应，其实 `health` 一直 200——端口活着，但模型路径根本没依赖。

根因：直接跑框架解释器，没带上 venv 的 site-packages。`/health` 不碰模型，所以测不出。

避免：服务启动**固定用 venv 解释器**（`..剧本围读/qwen3-tts-test/.venv/bin/python`）；验证不能只看 `/health`，要真打一次 `/tts`。外发时要把“对方环境的解释器/依赖”一并说清。

**2. `nohup &` 起的服务被会话回收**

用 `nohup ... &` 在 exec 里起后台服务，exec 一结束进程就被杀——前端和 edge-tts 先后“莫名”挂过。我后来用 `subprocess.Popen(..., start_new_session=True)` 才稳定。

根因：后台进程挂在执行会话的进程组里，会话清理时一起被杀。

避免：用 `start_new_session=True`（或 launchd/service/`brew services`）脱离会话；别用前台 shell 的 `&`。也说明“进程在监听”不等于“能长期存活”。

**3. `pkill -f` 自我匹配，把执行命令的 shell 也杀了**

清理测试进程时 `pkill -f "xxx.mjs"`，命令串里含同样字符串，把当前 shell 一起干掉，后续命令没跑。我一度以为是脚本卡死，其实是自毁。

避免：用不会匹配自身的正则（`pkill -f 'xxx_[m]js'`）或直接按 PID；别在命令里出现和要杀进程相同的标识。

## 并发与锁（Qwen）

**4. `BoundedSemaphore` 门闩导致“永久堵死”**

第一次给 Qwen 池加信号量限并发，结果某次生成卡住后**一直占着唯一通行证不释放**，之后所有 `/tts` 永久等待（进程 0% CPU、30 个线程全在等锁）。这比“瞬时内存峰值”更糟。

根因：门闩的释放只挂在“正常/异常返回”上；生成一旦**无限挂起**（既不返回也不抛错），就没人释放。BoundedSemaphore 的计数只进不出。

避免：任何“占用共享资源”的锁都要**成功/异常都在 finally 释放**；acquire 要带**超时**（`QWEN_ACQUIRE_TIMEOUT`），让卡住时请求到点拿 503 而不是无限等。

**5. 模型在服务端偶发挂起（最深的坑，尚未根治）**

最诡异的一个：模型在**隔离测试**里单线程、跨线程连续生成都正常（3 次、2 次都 OK），但在 `ThreadingHTTPServer` 里**第二次生成偶发卡住**（0% CPU）。靠 acquire 超时兜底后不再永久堵死，但根因没查透。

根因：指向 mlx/transformers 的 `generate` 与“每个请求一个线程”的服务交互（线程切换/资源竞争），隔离环境复现不了。

避免（外发前必须处理）：把生成**串行化到单一 worker 线程**（每次都在同一线程调模型，规避线程交互），或**子进程隔离 + 超时强杀**。单靠信号量治标不治本。排查时先用“隔离同参调用”排除模型本身，再怀疑线程/服务层。

## React 状态时序

**6. 读“刚 set 的 state”拿到旧值（出现两次）**

`analyze()` 里先 `setGenderSel(...)`，紧接着读 `genderSel` 做覆盖；因为 `setState` 异步，读到的是旧空对象，把新角色刚设的性别/年龄清空了（第二集新角色没默认值）。`seriesId` 同理——Qwen 种子预填用了 state 的 `seriesId`（还是空串），导致“复用”分支从不执行，Qwen 复用其实在重生成。

根因：把“下一步逻辑”建立在“state 已经更新”的假设上。

避免：`setState` 后**不要立刻读该 state**；要用的值**先在函数里算好**（局部变量），或抽成纯函数一次性算（我抽了 `applyGenderAge`、改用局部 `sid`）。这是 React 高频坑。

## 数据模型（跨集音色库）

**7. 音色库“整库覆盖”，前几集角色被冲掉**

`start()` 只用当前集角色 `buildBankRoles` 然后 `saveVoiceBank` **整体覆盖**，第二集没有聂九罗就把她冲没了（她的种子文件其实还在，只是 bank 记录丢了）。排查时发现 bank 里只剩第二集角色，还混进一个 `熊黑迟疑片刻` 的幽灵角色。

根因：把“累积的跨集数据”当成“本集快照”来写。

避免：**跨集/累积数据必须“合并”而非“替换”**——先读现有再 `{...existing, ...incoming}`；并配回归测试（我加了 `mergeVoiceBanks` 测试）。幽灵角色则靠 `normalizeRoleName` 补“状态+片刻”尾缀归并。

## 编辑过程（重复两次的坑）

**8. 批量替换误伤了变量定义（`--shadow-card`、`--select-arrow`）**

用 perl/Python 做“硬编码颜色→变量”批量替换时，把 `--shadow-card`、`--select-arrow` 的**定义值**（其值正好也是被替换的字符串）也换成了 `var(--自身)`，变成自引用，导致浅色下该变量失效、卡片阴影/下拉箭头消失。而且**我在 `--shadow-card` 踩了一次，后面 `--select-arrow` 又踩了一次**。

根因：批量替换按“值”匹配，没区分“定义行”和“使用行”。CSS 自定义属性值恰好等于被替换内容时就会中招。

避免：**批量替换后立即校验定义区没有 `var(--自身)`**；或替换时排除定义行；主题色尽量“单一来源”，别让值散落得让批量替换容易误伤。也说明要有一个“改完必须验证”的守卫意识。

## UI / 主题细节

**9. 顶部栏磨砂玻璃“没质感”**

围读页顶部栏明明加了 `backdrop-filter` 却看不出透明。因为顶部栏是 flex 列的第一个子元素，正文在它**下方**滚动，根本不会从它下面穿过，没东西可模糊。

根因：磨砂玻璃需要“有内容从下面滚过”，而顶部栏没有悬浮在正文上方。

避免：把顶部栏改成 `position: fixed`（限定 `.player-page .topbar`），正文铺满并留 padding-top，内容才能滚到下面产生磨砂。

**10. 下拉三角颜色、卡片阴影深色下失效**

`select` 用了 `appearance:none` + 硬编码 `#1d1d1f` 的 SVG 箭头，深色下“深箭头配黑底”看不见；卡片阴影用黑色 `--shadow-card`，黑底上就是没有投影。

根因：颜色写死、且“黑色阴影”在黑背景上天然不可见。

避免：下划线/图标类颜色抽成**随主题变**的变量（`--select-arrow` 深色给浅值）；深色模式下卡片/弹窗用“亮边 + 光晕”或浅色阴影来体现高度，不能沿用黑阴影。

**11. 弹窗对齐目标选错**

上传页设置弹窗先对齐到了“上传文本框(upload-zone)”中心，但文本框在卡片上半部，导致弹窗整体偏上、顶端冒出卡片。改成与**整个上传卡片**中心对齐才对称。

根因：对“对齐”的参照物判断不准确，只量了局部（文本框）而没看整体（卡片）。

避免：做布局对齐时，先量清楚“参照整体”的边界/中心，用计算样式和 `getBoundingClientRect` 量测，别靠目测；改完截图 + 量 `diff` 复核。

## 可复用的方法论

这次几次难啃的 bug 靠的不是猜，而是：
- **隔离测试**：把“我的代码”和“模型/库”分开（独立脚本证明模型正常，定位到服务线程交互）。
- **真实浏览器复现**：用 CDP 读 `getComputedStyle` / `getBoundingClientRect`，而不是静态分析。
- **量测而不是目测**：弹窗偏移、卡片中心、下划线颜色，都是量出的差值。
- **抽纯函数 + 单测**：`roleBase`/`isPersona`/`sortRolesForConfirm`/`mergeVoiceBanks` 都加了回归护栏。

## 精简版“以后可套用”原则

1. 服务启动：固定 venv 解释器 + `start_new_session` 脱离会话；验证要真打一次接口，不能只看 `/health`。
2. 任何锁：`finally` 释放 + acquire 带超时；跨线程调模型优先“单一 worker 线程”或子进程隔离。
3. React：`setState` 后不立刻读该 state；要用的值先在函数里算好/抽纯函数。
4. 累积数据：合并不覆盖 + 配回归测试。
5. 批量改代码后：校验定义区无自引用；改完跑 typecheck/test/build 并浏览器量测关键点。
6. “偶发、只在特定环境复现”的 bug：先隔离测试排除自己代码，再怀疑三方库；用真实环境 + 帧/样式量测复现。

## 当前仍待处理

- **Qwen 服务端偶发挂起**（最影响实际使用，外发前建议按“单一 worker 线程”或“子进程隔离”处理）。
- `index.css` 里 `--dark` 变量未使用、`svc-dot` 三个状态色仍是硬编码（可变量化，非 bug）。
- 外发打包：依赖前端 build + 3 个 Python 服务 + Qwen 模型/edge 依赖 + 对方环境；存档目录按用户懒创建、不随软件走（详见 `HANDOFF.md`、`外发说明.md`）。
