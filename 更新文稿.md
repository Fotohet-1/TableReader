# TableReader 更新说明 · v1.1.0

> 对比基线：上一版正式外发包 `TableReader-正式版-v1.0.0.zip`。本次为 minor 升级（新增功能、向后兼容），存档结构与数据不变。

## 一、版本对比

| | v1.0.0 | v1.1.0 |
|---|---|---|
| 整集完整音频 | 无 | 自动拼接整集音频到项目目录，角标提示 |
| 桌面 App 化 | 无 | PWA 无地址栏窗口 + 启动器 `.app` |
| DeepSeek Key | 明文输入框 | 默认密文 + 眼睛切换，保存后不露全文 |
| 应用名 | 剧本围读 | TableReader |
| 应用图标 | 默认生成 | 自定义渐变蓝 + macOS 标准满铺圆角 |
| PWA 顶栏 | 蓝色 | 与页面同款灰白 `#fcfcfc` |

## 二、新增

### 1. 整集完整音频自动生成
- 某集全部句子合成完成且零失败后，自动按播放时间轴拼接出 `{工作区}/{剧集}/{集}/{剧名} {集名} 完整音频.wav`；同集同时说话的句子会重叠拼接。
- 围读页右上角角标状态流转：`合成中 → 正在生成整集音频… → 整集音频已生成`。生成后可点击在访达中显示整集音频。
- 生成失败显示“整集音频生成失败”，点击可重试。
- 继续围读旧集：若整集已完整但还没拼过，角标会显示“可生成整集音频”；重生成某句后自动重拼。
- 存档选择列表里，已有完整音频的集显示“完整音频”标记。

### 2. 桌面 App 化
- 支持 PWA：Chrome 打开后点“安装”，或 Safari「文件 → 添加到 Dock」，得到带图标、无地址栏的独立窗口。
- 新增启动器 `TableReader.app`：双击即可拉起四个本地服务并打开前端，替代 `start.command`。
- 应用图标已更换为自定义渐变蓝设计，并按 macOS 标准满铺圆角重做，Dock 里大小观感正常。

### 3. DeepSeek Key 密文显示
- 设置与引导页的 Key 输入框默认密文，右侧眼睛点击切换明文。
- 保存后不再明文展示，避免屏幕共享/录屏泄露；设置弹窗已保留“清除 Key”。

## 三、调整
- 应用名统一改为 `TableReader`：窗口标题/菜单栏、PWA manifest、启动器 Info.plist、首页主标题。
- PWA 顶栏颜色改为页面同款灰白 `#fcfcfc`。
- 首页主标题由“剧本围读助手”改为 `TableReader`。

## 四、说明
- 版本号：`v1.0.0 → v1.1.0`（新增功能、向后兼容）。
- 存档目录与数据结构未变，旧存档可直接继续使用。
- 已安装过的 PWA 或已拖入 Dock 的 `.app` 可能缓存旧名称/图标/顶栏颜色，需重新安装或重新拖入才会刷新。

## 五、内部
- 整集音频拼接按播放器时间轴实现（同 `group` 重叠、其余按序），非重叠段用切片拷贝，长剧拼接更快。
- GUI 自检脚本适配“首页只能点按钮进入”，并支持临时存档目录，跑自检不再污染真实存档。
