# TableReader 更新说明 · v1.2.3

> 对比基线：上一版正式外发包 `TableReader-正式版-v1.2.2.zip`。本次为 patch 升级（界面优化，存档结构与数据不变）。

## 新增

### 围读页顶栏显示当前文件名称
- 围读页返回键右侧新增"正在围读 + 文件名"，显示当前正在围读的集名，例如《火旺》E04-260815。
- 文件名过长时自动省略，鼠标悬停可查看完整名称。
- 手机窄屏隐藏"正在围读"标签，只保留文件名，避免顶栏拥挤。

## 说明

- 版本号：`v1.2.2 → v1.2.3`。
- 应用功能、存档目录与数据结构未变，旧存档可直接继续使用。
- 已生成的 `TableReader.app` 与 `dist/` 已按 v1.2.3 重建。
