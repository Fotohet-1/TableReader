# 剧本围读助手 · 搭建复盘（part1）

> 覆盖本轮从头搭到能用的完整过程，按“难点 → 根因 → 怎么避免”整理，最后给几条以后可直接套用的原则。配合 `HANDOFF.md`、`README.md` 一起看。

## 解析与角色

**1. 联动说话拆得过于激进**

加了“两人同时说话”后，我用空白符当拆分符，结果 `聂九罗 董事长` 被拆成 `聂九罗` 和 `董事长` 两个角色，`炎拓 总裁` 同理。这类“角色名+头衔/动作”本应归成一个角色下的变体。另外我一度直接用正则从原始行重新截取角色名，遇到带括号、`OS`、斜杠的写法就匹配失败，外栏只剩灰框。

避免：显示层直接用解析好的 `u.character / u.text`，别再二次正则；拆分只认 `/ 、 ， + & 和` 这些明确连词，绝不拿空白符拆；每种角色写法（括号、OS、异口同声、头衔空格）都留一句回归用例。

**2. `groupRoles` 归并方向单一**

前缀归并只认“短名在前、长变体在后”：`熊黑` 在前才能并掉 `熊黑严肃`；反过来 `炎拓 总裁` 先出现，`炎拓` 就被拆成独立角色。

避免：改成双向归并（短名是长名的前缀就归并），并统一以短名/基底名为规范名。已在 `HANDOFF.md` 记为待优化项。

## 播放与音频

**3. `content-visibility` 把外栏角色名裁掉了**

为了长剧本性能给每句加了 `content-visibility: auto`，它自带 `contain: paint`，会把位于元素框外的绝对定位子元素裁掉。角色名放在台词块外侧 `left:-80px`，几何上“存在”，实际被绘制裁剪，只看到灰色竖线。

避免：不要依赖会溢出元素的装饰；要么让角色名留在元素内（后来改成内部左留白），要么关掉该句的 content-visibility。也提醒：光量 `getBoundingClientRect` 会骗人，必须看真实渲染截图。

**4. 续读位置里藏着偏移 bug**

旧播放页把 `initialMs/1000` 直接当句内偏移用，正确应是 `initialMs - slot.startMs`。只有续读到靠后的句子才暴露。

避免：续读恢复的数学单独写、单独测；不只测“从头续”。

**5. 非存档模式的内存泄漏**

不用存档时每句 `URL.createObjectURL` 从不回收，试听/音色库换音频也不 revoke 旧的。

避免：换源/卸载时回收 blob；把存档 URL（确定性、无 blob）作为主路径；用 ref 记录自己拥有的 blob。

**6. 测试浏览器媒体管线崩了，白排查很久**

重构后音频一直卡在 readyState 0，我一度怀疑是服务端 Range/WAV 头问题，逐层排查。根因是那个跑了大半天的测试 Chrome 媒体管线坏了（反复 `Trying to load the allocator multiple times`），换一个全新 Chrome 同一个字节立刻能播。

避免：先怀疑“是不是测试环境坏了”，用新开浏览器快速验证；不要在一个可疑环境里深挖代码。

## 存档与状态

**7. 存档拆分把旧数据弄丢（最严重）**

把 meta 拆成 `project.json`（剧本/单元/角色）+ `meta.json`（音频/进度）后，`/playback` 的 `write_meta` 只保留状态字段，把旧单文件 meta 里的 `scriptText/units/voices` 直接丢掉，导致续读变成空页（`units=0`），还不好发现（列表仍有名字、有音频）。

避免：任何一次写入都保留未知/旧字段，或做显式迁移（读旧 meta → 一次性写 project.json）；schema 变更前先想清楚对已有数据的影响；改完拿一个旧存档实测。

## 样式与设计

**8. 全局基类样式把覆盖“顶”回去**

我给 `.choose-card` 删了边框，但它是 `button`，全局 `button { border }` 又给它加回来；首页“开始使用”悬停文字变蓝，是全局 `button:hover { color: accent }` 覆盖；`多种表述` 下拉被 `.cv-row select { flex:1 }` 拉伸到不等宽；所有下拉的箭头还要 `!important` + 加内边距才不与文字重叠。

避免：覆盖基类元素时，别只删自己那份声明，要显式 `border:0 / color` 压住基类；用“class+后代”这种更高优先级去覆盖，别依赖书写顺序；动全局按钮/下拉前先扫一遍已有的基类规则。

**9. 术语/页面位置理解偏差**

“开始围读”我一度当成了播放页，把播放页返回键删了，其实指的是“上传/继续”那个选择页，绕了一圈又加回来。

避免：涉及具体页面/控件时，用截图或精确到“哪一页/哪个按钮”说清楚；吃不准先确认一句。

## 工程与协作

**10. 长对话上下文要爆**

这轮迭代非常长，上下文接近上限。靠每步 git 提交 + 最后写的 `HANDOFF.md` + 系统记忆兜底，基本无损失。

避免：每个里程碑都提交；定期刷新交接文档；大的feature wave 单独开新对话，第一句指向仓库和 HANDOFF。

**11. 测试 Chrome 页面目标会消失**

e2e 偶尔报“未找到页面目标”，因为远程调试端口下没有 page，需要先 `PUT /json/new`。

避免：写一个 bootstrap 帮助函数，跑 e2e 前自动重建页面目标；或改用有持久上下文的测试框架。

## 可长期套用的原则

- 以真实渲染为准，别只看计算样式/矩形。
- 改动数据结构前，先确认旧数据兼容，写迁移而非“就地覆盖”。
- 启发式拆分要保守，并配回归用例；宁可少拆，不要错拆。
- 覆盖基类样式要用显式值 + 足够优先级，别赌书写顺序。
- 先把“是不是环境坏了”排除掉，再深挖代码。
- 提交要频繁 + 写交接文档，长项目靠 git 而不是靠上下文。

## 一句话总结

这轮最大的教训是两件：**存档迁移丢数据**、**用 content-visibility 和启发式拆分时没想清边界**。本质都是“改动某处时没评估它对已有状态/其他规则的影响”。以后动这类跨层逻辑，我会先写一个针对旧数据和边界的小测试再改。
