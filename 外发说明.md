# 剧本围读助手 · 给接收方的说明

配套使用指南：打开 `docs/核心功能与使用技巧/核心功能与使用技巧-landing.pdf`（网页风格版，界面截图已更新），了解核心功能、整体流程与使用技巧。

## 你需要准备

- Apple Silicon Mac（M1 / M2 / M3 / M4）
- 能上网：edge-tts 用微软在线音色，Qwen3 模型从 hf-mirror 下载
- 磁盘剩余约 7GB
- Python 3.11：安装脚本会检查；没有就先执行 `brew install python@3.11`

## 第一次使用

1. 双击 `setup.command`。如果 macOS 提示“无法打开”，右键点文件再选“打开”。
2. 等终端自动完成四步：装 edge-tts 依赖、装 Qwen3 依赖、下载约 4.5GB 模型（可断点续传）、检查前端产物。第一次通常要 15-30 分钟。
3. 双击 `start.command`，浏览器会自动打开 `http://127.0.0.1:5174/`。

如果对方习惯用 AI agent 代装，把本文件夹里的 `AGENT_SETUP_RECIPIENT.md` 交给对方 agent 即可，agent 会自己完成安装、启动和验证。

## 以后使用

双击 `start.command` 即可。已在运行的端口会自动跳过，不会重复启动；服务日志在 `logs/`。

## 存档说明

- 第一次合成时，存档服务会在“文稿（Documents）/剧本围读存档”里自动创建你的存档，纯本地。
- 换电脑或重装不会带走存档；想备份，把整个“剧本围读存档”文件夹拷走。
- 想接手别人共享的存档，把整个“剧本围读存档”文件夹放进你的文稿目录即可。

## 常见问题

- Qwen 首次生成音色要加载模型，等几十秒是正常的；之后同模型复用。
- edge-tts 需要联网；Qwen3 全程本地。
- 端口被占用时脚本会自动跳过，不影响使用。
- 卸载：删掉整个应用文件夹，再删掉 `~/Documents/剧本围读存档`。
