# TableReader 更新说明 · v1.2.1

> 对比基线：上一版正式外发包 `TableReader-正式版-v1.2.0.zip`。本次为 patch 升级（修复问题，存档结构与数据不变）。

## 修复

- 修复外发包里 README 截图路径失效：`cover/assets/` 不入包，README 改为引用包内已有的 `docs/assets/` 截图，仓库与外发包里的 README 都能正常显示。

## 说明

- 版本号：`v1.2.0 → v1.2.1`。
- 应用功能、存档目录与数据结构未变，旧存档可直接继续使用。
- 已生成的 `TableReader.app` 与 `dist/` 已按 v1.2.1 重建。
